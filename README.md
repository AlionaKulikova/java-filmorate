# Filmorate

Добро пожаловать в проект Filmorate! Это Spring Boot приложение для управления фильмами и пользователями, позволяющее добавлять, обновлять и оценивать фильмы, управлять друзьями и лайками, а также получать рекомендации по топ-фильмам. Проект развивался через несколько спринтов, добавляя функции вроде валидации, бизнес-логики, REST API и интеграции с базой данных H2. Теперь вы можете легко выбрать фильм для уютного вечера! 🍿

## Описание проекта

Filmorate — это сервис для любителей кино, вдохновленный платформами вроде IMDb или Kinopoisk. Пользователи могут регистрироваться, добавлять фильмы, ставить лайки, добавлять друзей и просматривать топ-фильмы по популярности. Приложение поддерживает валидацию данных, хранение в памяти или базе данных, и полный REST API для интеграции.

Проект построен с использованием Spring Boot, принципов ООП, внедрения зависимостей и покрыт тестами. В финальной версии добавлена база данных H2 для持久 хранения данных. 🚀

## Основная функциональность

### Модели данных:
- **Film**: id, name, description, releaseDate, duration, mpa (рейтинг), genres (список жанров).
- **User**: id, email, login, name, birthday.
- **Mpa**: id, name (рейтинг, например, G, PG и т.д.).
- **Genre**: id, name (жанр, например, Комедия).

### Хранение данных:
- Изначально: InMemoryFilmStorage и InMemoryUserStorage (в памяти).
- Финальная версия: UserDbStorage и FilmDbStorage (DAO с базой H2).

### Сервисы:
- **UserService**: Управление друзьями (добавление, удаление, общие друзья).
- **FilmService**: Управление лайками, получение топ-фильмов по лайкам.

### Валидация:
- Film: name не пустое, description ≤ 200 символов, releaseDate ≥ 1895-12-28, duration > 0.
- User: email не пустой и содержит @, login не пустой без пробелов, name может быть пустым (используется login), birthday не в будущем.
- Дополнительно: Использование аннотаций @Valid, @NotNull, @Email для автоматической валидации.

### REST API:
- Полный набор эндпоинтов для CRUD операций, друзей, лайков, жанров и MPA.
- HTTP-коды: 200 (успех), 400 (валидация), 404 (не найдено), 500 (ошибка).

### Логирование:
- Логирование операций добавления/обновления и ошибок.
- Интеграция Logbook для логирования HTTP-запросов/ответов.

### Тестирование:
- Unit-тесты для валидации.
- Интеграционные тесты для DAO с H2 в памяти.

## Архитектура проекта

### Пакеты:
- **model**: Классы Film, User, Mpa, Genre.
- **storage**: Интерфейсы FilmStorage, UserStorage; реализации InMemoryFilmStorage, InMemoryUserStorage, UserDbStorage, FilmDbStorage.
- **service**: UserService, FilmService.
- **controller**: FilmController, UserController.
- **exception**: Обработка ошибок (ValidationException и т.д.).

### Зависимости:
- Spring Boot (Web, Validation, JDBC).
- H2 Database.
- Lombok для @Data.
- Logbook для логирования HTTP.
- JUnit для тестирования.

Проект использует паттерны вроде Dependency Injection (@Autowired), DAO для доступа к данным и REST для API.

Template repository for Filmorate project.
#
#
![database_scheme](/database_scheme.svg)

## Установка и запуск

Клонируйте репозиторий:

```bash
git clone https://github.com/AlionaKulikova/java-filmorate.git
cd filmorate


